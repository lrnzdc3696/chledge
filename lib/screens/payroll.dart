import 'package:flutter/material.dart';

class Payroll extends StatelessWidget {
  const Payroll({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("This in payroll"));
  }
}
